import { Routes } from '@angular/router';
import { JustDirectiveComponent } from './just-directive.component';

export const justDirectiveRoutes:Routes=[
	{
        path:'',
        component:JustDirectiveComponent,
    }
];